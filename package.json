{
  "name": "root",
  "private": true,
  "main": "package.json",
  "scripts": {
    "dev": "lerna run dev --parallel",
    "testprod": "lerna run testprod --parallel",
    "build": "lerna run build --parallel",
    "install-all": "lerna run install-all --parallel"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
